<%= simple_form_for(@restaurant) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number %>
  <%= f.input :category, :collection => %w[chinese italian japanese french belgian] %>
  <%= f.button :submit, value: "Add restaurant", class: "btn btn-info" %>
<% end %>
