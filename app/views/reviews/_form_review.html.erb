<%= simple_form_for [ @restaurant, @review ] do |f| %>
  <%= f.error_notification %>
  <%= f.input :content %>
  <%= f.input :rating, :collection => %w[0 1 2 3 4 5] %>
  <%= f.button :submit, value: "Post review", class: "btn btn-info" %>
<% end %>
